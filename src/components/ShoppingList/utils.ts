import { ContentStyle } from '@shopify/flash-list';
import { verticalScale } from '~/helpers/metrics';

export const flashListContentContainer: ContentStyle = {
  paddingBottom: verticalScale(40),
};
